# Surely

# Usage

Get the gem:

```shell
$ gem install surely
$ cd surely
```

Configure it:

```
export DIRECTORY=/path/to/screenshots
export IMGUR_CLIENT_ID=…
export IMGUR_CLIENT_SECRET=…
```

Start the daemon:

```
$ surely
```
